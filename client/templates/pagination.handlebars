<nav aria-label="Page navigation">
    <ul class="pagination custom">
        {{#each nav_Content}}
        <li class="customLi">
            <a
            {{#if target_Page_Offset}} 
                href="#/paginacalls/{{target_Page_Offset}}" aria-hidden="false" aria-label="{{arias_Description}}"
            {{else}}
                aria-hidden="true"
            {{/if}}>
                {{user_Visible}}
            </a>
        </li>
        {{/each}}
    </ul>
</nav>